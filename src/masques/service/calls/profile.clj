(ns masques.service.calls.profile
  (:require ;[masques.model.friend :as friend-model]
            [masques.service.actions.profile :as profile-action]
            [masques.service.core :as service-core]))

(defn profile [friend]
  ;(:data (service-core/send-message (friend-model/destination friend) profile-action/action))
  )