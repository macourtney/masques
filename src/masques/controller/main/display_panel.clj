(ns masques.controller.main.display-panel
  (:require [clojure.tools.logging :as logging]
            [masques.controller.utils :as controller-utils]
            [masques.view.main.main-frame :as view-main-frame]))

(defn init [display-panel]
  )